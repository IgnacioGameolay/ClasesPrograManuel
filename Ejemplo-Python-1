# Clase del objeto "Persona"


class Persona:
    # Constructor (¿Qué necesito para "Construir" a mi "Persona"?)
    def __init__(self, nombre, edad):
        # Atributos (¿Qué características tiene mi "Persona"?)
        self.nombre = nombre
        self.edad = edad
        # Color... Altura... etc..

    # Métodos (¿Qué puede hacer la clase "Persona"?)
    def saludar(self):
        print(f"Hola, mi nombre es {self.nombre} y tengo {self.edad} años.")

    def es_mayor_de_edad(self):
        return self.edad >= 18



### MAIN ###
def sumar_hasta(n):
    suma = 0
    for i in range(1, n + 1):
        suma += i
    return suma

#
nombre = "Igna"
edad = 21
numero = 5

#
print("=== Datos básicos ===")
print("Nombre:", nombre)
print("Edad:", edad)

if edad >= 18: print("Es mayor de edad")
else: print("Es menor de edad")

print("Suma hasta", numero, "=", sumar_hasta(numero))


## Defino e Instancio un objeto de clase "Persona". | declarar, definir, instanciar
persona1 = Persona(nombre, edad)
persona2 = Persona("Manuel", 20)

persona2.saludar()
